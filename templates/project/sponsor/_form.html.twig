{{ form_start(form) }}
    <div class="form-group col-md-12">
        <label>{{ form_label(form.organization) }}</label>
        {{ form_widget(form.organization,{'attr':{'class':'form-control'}}) }}
        <div class="help-block with-errors"></div>
    </div>
    <div class="form-group col-md-12">
        <label>{{ form_label(form.type) }}</label>
        {{ form_widget(form.type,{'attr':{'class':'form-control'}}) }}
        <div class="help-block with-errors"></div>
    </div>
    <div class="form-group col-md-12">
        <label>{{ form_label(form.additional_info) }}</label>
        {{ form_widget(form.additional_info,{'attr':{'class':'form-control wyiswig'}}) }}
        <div class="help-block with-errors"></div>
    </div>
{% if not button_label is defined  %}
    <button class="btn btn-info">{{ button_label|default('Save') }}</button>
    <button class="btn  btn-warning mr-3" type="reset">Reset</button>

{% else %}
    <input type="hidden" name="edit" value="{{ edit }}">
    <a href="{{ path('project_sponsor_index', {'project': project.id }) }}" class="btn btn-primary">
        Cancel
    </a>

    <button class="btn btn-success mr-2">{{ button_label|default('Save') }}</button>
{% endif %}
{{ form_end(form) }}